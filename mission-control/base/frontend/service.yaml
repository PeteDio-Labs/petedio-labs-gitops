apiVersion: v1
kind: Service
metadata:
  name: mission-control-frontend
  namespace: mission-control
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: mission-control-frontend
