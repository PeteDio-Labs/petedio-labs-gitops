apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Sealed secrets for mission-control dev environment
# Generated by: ./scripts/generate-sealed-secrets.sh dev
# These are encrypted and safe to commit to Git

resources:
  - proxmox-token.yaml
  - gemini-api-key.yaml
  - auth-token.yaml
  - postgres-password.yaml
