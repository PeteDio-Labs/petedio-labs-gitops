apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: discord-bot
  namespace: argocd
  annotations:
    argocd-image-updater.argoproj.io/image-list: bot=docker.toastedbytes.com/discord-ai-bot
    argocd-image-updater.argoproj.io/bot.update-strategy: latest
    argocd-image-updater.argoproj.io/bot.allow-tags: regexp:^latest$
    argocd-image-updater.argoproj.io/write-back-method: git
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/petedillo/discord-bot-gitops
    targetRevision: main
    path: kubernetes/overlays/prod
  destination:
    server: https://kubernetes.default.svc
    namespace: discord-bot
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
