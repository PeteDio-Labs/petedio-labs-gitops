apiVersion: v1
kind: Service
metadata:
  name: web-search-service
  labels:
    app: web-search-service
spec:
  type: ClusterIP
  ports:
  - port: 3003
    targetPort: 3003
    protocol: TCP
    name: http
  selector:
    app: web-search-service
